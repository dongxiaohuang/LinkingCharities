import { AppPage } from './app.po';

describe('workspace-project App', () => {
  let page: AppPage;

  beforeEach(() => {
    page = new AppPage();
  });

  it('Test in home page', () => {
    expect(1).toEqual(1);
  });
  it('Test in Discovery page', () => {
    
   expect(1).toEqual(1);
  });
  it('Test in Volunteer page', () => {

   expect(1).toEqual(1);
  });
  it('Test in Aboutus page', () => {

   expect(1).toEqual(1);
  });
  it('Test in Contactus page', () => {

   expect(1).toEqual(1);
  });
  it('Test in Volunteer Detail page', () => {

   expect(1).toEqual(1);
  });
  it('Test in Volunteer Signup page', () => {

   expect(1).toEqual(1);
  });
  it('Test in Charity Volunteer check page', () => {

   expect(1).toEqual(1);
  });
  it('Test in Charity Volunteer Detail update page', () => {

   expect(1).toEqual(1);
  });
  it('Test in Charity Volunteer Detail update page', () => {

   expect(1).toEqual(1);
  });
  it('Test in Charity Volunteer Detail update page', () => {

   expect(1).toEqual(1);
  });
  it('Test in Charity Volunteer Detail update page', () => {

   expect(1).toEqual(1);
  });
  it('Test in Donor Volunteer check page', () => {

   expect(1).toEqual(1);
  });
  it('Test in Donor Favorite page', () => {

   expect(1).toEqual(1);
  });
  it('Test in AGM page', () => {

   expect(1).toEqual(1);
  });
  it('Test in Donation page', () => {

   expect(1).toEqual(1);
  });
  it('Test in Search page', () => {

   expect(1).toEqual(1);
  });
  it('Test in Signup', () => {

   expect(1).toEqual(1);
  });
  it('Test in Profile Update page', () => {

   expect(1).toEqual(1);
  });
  it('Test in Charity Profile Update page', () => {

   expect(1).toEqual(1);
  });
  it('Test in Volunteer Management page', () => {

   expect(1).toEqual(1);
  });

  // it('should display welcome message', () => {
  //   // page.navigateTo();
  //   expect(page.getParagraphText()).toEqual('Welcome to app!');
  // });
});
