import { AddBgColorDirective } from './add-bg-color.directive';

describe('AddBgColorDirective', () => {
  it('should create an instance', () => {
    // const directive = new AddBgColorDirective();
    // expect(directive).toBeTruthy();
  });
});
